+ipcMain.handle('master:save',async(_e,doc)=>{const p=doc?.payload||{};const safe=String(p.customerName||'customer').replace(/[^\p{L}\p{N}_-]+/gu,'_');const out=await dialog.showSaveDialog(win,{title:'Save Universal CAD Entitlement',defaultPath:`UCAD-${safe}-${p.editionName||'Edition'}-${p.seats||1}Seats.ucadent`,filters:[{name:'Universal CAD Entitlement',extensions:['ucadent']}]});if(out.canceled||!out.filePath)return{canceled:true};fs.writeFileSync(out.filePath,serializeEntitlement(doc),{encoding:'utf8',mode:0o600});return{canceled:false,filePath:out.filePath};});
 
-ipcMain.handle('master:generate-owner',(_e,input)=>{const ownerName=String(input?.ownerName||'Owner').trim()||'Owner';const validFrom=String(input?.validFrom||'');const validUntil=String(input?.validUntil||'');const password=String(input?.masterPassword||'');if(!/^\d{4}-\d{2}-\d{2}$/.test(validFrom)||!/^\d{4}-\d{2}-\d{2}$/.test(validUntil)||validUntil<validFrom)throw new Error('ช่วงวันที่ Owner Universal License ไม่ถูกต้อง');const masterPrivate=decryptPrivateKey(readMasterDocument(),password);return createOwnerLicense({licenseId:crypto.randomUUID(),ownerName,productDisplayName:'Universal CAD Studio Owner',editionName:'Owner Universal',validFrom,validUntil,suiteVersion:SUITE_VERSION,engineVersion:ENGINE_VERSION,issuedAt:new Date().toISOString(),issuer:'Universal CAD Master License Manager',nonce:crypto.randomBytes(18).toString('base64url')},masterPrivate);});
+ipcMain.handle('master:generate-owner',(_e,input)=>{const ownerName=String(input?.ownerName||'Owner').trim()||'Owner';const validFrom=String(input?.validFrom||'');const validUntil=String(input?.validUntil||'');const password=String(input?.masterPassword||'');if(!/^\d{4}-\d{2}-\d{2}$/.test(validFrom)||!/^\d{4}-\d{2}-\d{2}$/.test(validUntil)||validUntil<validFrom)throw new Error('Invalid Owner Universal License validity period.');const masterPrivate=decryptPrivateKey(readMasterDocument(),password);return createOwnerLicense({licenseId:crypto.randomUUID(),ownerName,productDisplayName:'Universal CAD Studio Owner',editionName:'Owner Universal',validFrom,validUntil,suiteVersion:SUITE_VERSION,engineVersion:ENGINE_VERSION,issuedAt:new Date().toISOString(),issuer:'Universal CAD Master License Manager',nonce:crypto.randomBytes(18).toString('base64url')},masterPrivate);});
 ipcMain.handle('master:save-owner',async(_e,doc)=>{const p=doc?.payload||{};const out=await dialog.showSaveDialog(win,{title:'Save Owner Universal License',defaultPath:`UCAD-OWNER-UNIVERSAL-${p.validUntil||'license'}.ucadlic`,filters:[{name:'Universal CAD Owner License',extensions:['ucadlic']}]});if(out.canceled||!out.filePath)return{canceled:true};fs.writeFileSync(out.filePath,serializeOwnerLicense(doc),{encoding:'utf8',mode:0o600});return{canceled:false,filePath:out.filePath};});
 
 ipcMain.handle('master:info',()=>({version:app.getVersion(),suiteVersion:SUITE_VERSION,engineVersion:ENGINE_VERSION,ownerUniversalLicense:true}));
diff --git a/master-license-manager/preload.cjs b/master-license-manager/preload.cjs
index 1e5682c..c81c0ff 100644
--- a/master-license-manager/preload.cjs
+++ b/master-license-manager/preload.cjs
@@ -1 +1 @@
-'use strict';const{contextBridge,ipcRenderer}=require('electron');contextBridge.exposeInMainWorld('masterApi',{keyStatus:()=>ipcRenderer.invoke('master:key-status'),importKey:()=>ipcRenderer.invoke('master:import-key'),catalog:()=>ipcRenderer.invoke('master:catalog'),generate:(x)=>ipcRenderer.invoke('master:generate',x),save:(x)=>ipcRenderer.invoke('master:save',x),generateOwner:(x)=>ipcRenderer.invoke('master:generate-owner',x),saveOwner:(x)=>ipcRenderer.invoke('master:save-owner',x),appInfo:()=>ipcRenderer.invoke('master:info')});
+'use strict';const{contextBridge,ipcRenderer}=require('electron');contextBridge.exposeInMainWorld('masterApi',{keyStatus:()=>ipcRenderer.invoke('master:key-status'),importKey:()=>ipcRenderer.invoke('master:import-key'),removeKey:()=>ipcRenderer.invoke('master:remove-key'),catalog:()=>ipcRenderer.invoke('master:catalog'),generate:(x)=>ipcRenderer.invoke('master:generate',x),save:(x)=>ipcRenderer.invoke('master:save',x),generateOwner:(x)=>ipcRenderer.invoke('master:generate-owner',x),saveOwner:(x)=>ipcRenderer.invoke('master:save-owner',x),appInfo:()=>ipcRenderer.invoke('master:info')});
diff --git a/master-license-manager/ui/app.js b/master-license-manager/ui/app.js
index 0d01622..3140e4a 100644
--- a/master-license-manager/ui/app.js
+++ b/master-license-manager/ui/app.js
@@ -1,6 +1,15 @@
-'use strict';const $=id=>document.getElementById(id);let catalog;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const val=id=>$(id).value.trim();function msg(t,ok=false){$('message').textContent=t;$('message').className='message'+(ok?' success':'');}function fmt(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}const now=new Date(),end=new Date();end.setFullYear(end.getFullYear()+1);$('validFrom').value=fmt(now);$('validUntil').value=fmt(end);$('ownerValidFrom').value=fmt(now);const ownerEnd=new Date();ownerEnd.setFullYear(ownerEnd.getFullYear()+10);$('ownerValidUntil').value=fmt(ownerEnd);
-async function keyStatus(){const s=await window.masterApi.keyStatus();$('keyStatus').textContent=s.installed?'✓ Master Key พร้อม':'ยังไม่ได้ Import Master Key';return s.installed;}function renderFeatures(){const edition=catalog.editions.find(x=>x.id===$('editionId').value)||catalog.editions[0];const enabled=new Set(edition.features);$('features').innerHTML=catalog.features.map(f=>`<label class="feature"><input type="checkbox" data-feature="${esc(f.id)}" ${enabled.has(f.id)?'checked':''} ><span><b>${esc(f.label)}</b><small>${esc(f.group)} · ${esc(f.description)}</small></span></label>`).join('');$('productDisplayName').value=edition.displayName;}
-(async()=>{catalog=await window.masterApi.catalog();$('editionId').innerHTML=catalog.editions.map(e=>`<option value="${e.id}">${e.name}</option>`).join('');renderFeatures();$('version').textContent=`Master License Manager v1.0.0 · Engine ${catalog.engineVersion}`;await keyStatus();})();$('editionId').addEventListener('change',renderFeatures);$('importKeyBtn').addEventListener('click',async()=>{try{const r=await window.masterApi.importKey();if(!r.canceled)msg('Import Master Key สำเร็จ',true);await keyStatus();}catch(e){msg(e.message);}});
-$('form').addEventListener('submit',async e=>{e.preventDefault();$('result').classList.add('hidden');try{if(!(await keyStatus()))throw new Error('กรุณา Import Master Key ก่อน');const features=[...document.querySelectorAll('[data-feature]:checked')].map(x=>x.dataset.feature);const doc=await window.masterApi.generate({enrollmentCode:val('enrollmentCode'),customerName:val('customerName'),customerId:val('customerId'),entitlementLabel:val('entitlementLabel'),seats:Number(val('seats')),validFrom:val('validFrom'),validUntil:val('validUntil'),editionId:val('editionId'),productDisplayName:val('productDisplayName'),features,masterPassword:val('masterPassword')});$('masterPassword').value='';const saved=await window.masterApi.save(doc);if(saved.canceled)return msg('สร้างแล้วแต่ยังไม่ได้บันทึกไฟล์');const p=doc.payload;$('result').innerHTML=`<div class="title"><span>✓</span><h2>Entitlement พร้อม</h2></div><div class="result-grid"><b>ลูกค้า</b><span>${esc(p.customerName)}</span><b>Edition</b><span>${esc(p.productDisplayName)}</span><b>Seats</b><span>${p.seats}</span><b>Features</b><span>${p.features.length}</span><b>Host</b><span>${esc(p.licenseHostId)}</span><b>ไฟล์</b><span>${esc(saved.filePath)}</span></div>`;$('result').classList.remove('hidden');msg('สร้าง Entitlement สำเร็จ',true);}catch(err){$('masterPassword').value='';msg(err.message||String(err));}});
-
-$('generateOwnerBtn').addEventListener('click',async()=>{try{if(!(await keyStatus()))throw new Error('กรุณา Import Master Key ก่อน');const password=$('ownerMasterPassword').value;if(!password)throw new Error('กรุณากรอก Master Password');const doc=await window.masterApi.generateOwner({ownerName:$('ownerName').value.trim(),validFrom:$('ownerValidFrom').value,validUntil:$('ownerValidUntil').value,masterPassword:password});$('ownerMasterPassword').value='';const saved=await window.masterApi.saveOwner(doc);if(!saved.canceled)msg(`สร้าง Owner Universal License สำเร็จ · ใช้ได้ทุกเครื่องและทุกฟีเจอร์`,true);}catch(e){$('ownerMasterPassword').value='';msg(e.message||String(e));}});
+'use strict';
+const $=id=>document.getElementById(id);let catalog;
+const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
+const val=id=>$(id).value.trim();
+function msg(t,ok=false){$('message').textContent=t;$('message').className='message'+(ok?' success':'');}
+function fmt(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}
